export declare class SlideToggle {
    slideToggle(el: string | HTMLElement, timingFn?: number): void;
    private getElement(elData);
    private getPrototypOfStrategy(height);
    private isElementSliding(el);
    private setElRequiredDataAndChangeAttr(el);
}
export declare const slideToggle: SlideToggle;
